# code.highlightJs

**Reuse:** highlight.js 语法高亮（本地 vendor，支持 `file://` 打开）

**Variants:** json, plaintext

**Bundle:** `code.css`

Copy `vendor/highlightjs/` beside your deck, then paste `head.html` into `<head>`.
Pair with `interactive/content-modal` for JSON modals.
